# Phase I Commit Plan

This file lists draft commit messages for the file groups produced during
Phase I of the GauNEGF documentation overhaul. Claude does not run git;
the user reviews diffs and runs commits in the order suggested below.

Suggested commit order:

1. API truth table (one commit)
2. Phase I findings + bugs + JSONs (one commit)
3. Group A doc fixes (one commit)
4. Group B doc fixes (one commit)
5. Group C doc fixes (one commit)
6. Docstring fixes (one commit covering D1+D2+D3)
7. Judgment fixes per file group (zero to several commits, one per group)

---

## Commit candidate: API truth table

Files: docs/superpowers/specs/api-truth.md

Suggested message:
docs(spec): add API truth table for Phase I correctness audit

Generated by haiku agent via AST-equivalent inspection of gauNEGF/*.py.
Single source of API ground truth for Phase I verifiers, Phase II
docstring writers, and Phase III guide writers. Versioned for traceability.

---

## Commit candidate: Phase I findings + bugs

Files: docs/superpowers/specs/phase-1-findings.md
       docs/superpowers/specs/phase-1-bugs.md
       docs/superpowers/specs/phase-1-findings/group-A.json
       docs/superpowers/specs/phase-1-findings/group-B.json
       docs/superpowers/specs/phase-1-findings/group-C.json
       docs/superpowers/specs/phase-1-findings/group-D1.json
       docs/superpowers/specs/phase-1-findings/group-D2.json
       docs/superpowers/specs/phase-1-findings/group-D3.json

Suggested message:
docs(spec): record Phase I doc audit findings

Six parallel haiku verifiers compared every code reference in the
in-scope docs against the API truth table. 47 total findings split into
mechanical (applied directly), judgment (user-approved), and code-bug
flags (deferred to user triage). Includes raw per-group JSON for
traceability and a phase-1-bugs.md inventory of incidental code-bug
findings for separate triage.

---

## Commit candidate: Group A doc fixes (README + examples)

Files: README.md, examples/SiNEGF.py, examples/IntegralDemo.ipynb

Suggested message:
docs: fix code references in README and examples to match current API

- README quickstart: setContacts now uses lContact/rContact kwargs
- examples/SiNEGF.py: added missing `from gauopen import QCBinAr as qcb`
  import (verifier proposed `import gauopen as qcb` which would have
  resolved qcb.BinAr to non-existent gauopen.BinAr; corrected to the
  canonical pattern used elsewhere in the codebase)
- examples/IntegralDemo.ipynb cell fefcf954: setSigma now uses
  lContact/rContact/sig kwargs

Mechanical fixes only; judgment + code-bug findings deferred to
separate commits per phase-1-judgment-fixes.md / phase-1-bugs.md.

Verifier note: the recheck after fixes confirmed zero new mechanical
mismatches introduced. Several "judgment" and "code_bug" findings from
the original audit persist intentionally -- these are user-decision
and user-triage items.

---

## Commit candidate: Group B doc fixes (sphinx top + theory)

Files: docs/source/quickstart.rst,
       docs/source/theory/negf_dft.rst,
       docs/source/theory/best_practices.rst

Suggested message:
docs(sphinx): fix setSigma calls in quickstart + theory pages

- quickstart.rst: setSigma now uses lContact/rContact/sig kwargs
- theory/negf_dft.rst: setSigma now uses kwargs (and explicit sig=-0.1j
  default since the original positional form was incomplete)
- theory/best_practices.rst: setSigma now uses kwargs

Mechanical fixes only. The verifier flagged 6 mechanical findings;
3 were genuine setSigma signature mismatches (applied) and 3 were
verifier no-ops (decorative additions of `spin=None` and reordering
of fermi/qV kwargs that work in either order; SKIPPED).

The remaining 11 judgment-level Group B findings (NEGFE construction
pattern, setContactBethe shape, SigmaCalculator with surfG object,
NEGF.setSigma with T parameter) require user adjudication and are
deferred to phase-1-judgment-fixes.md.

Group B installation.rst, theory/introduction.rst, theory/transport.rst
were audited but had no mechanical findings to apply.

---

## Commit candidate: Group C doc fixes (sphinx examples)

Files: docs/source/examples/silicon_nanowire.rst,
       docs/source/examples/advanced_examples.rst

Suggested message:
docs(sphinx): fix code references in examples pages

- silicon_nanowire.rst: NEGFE construction now uses the canonical
  NEGFE(NEGF(...)) wrapping pattern instead of attempting to pass
  fn/func/basis directly to NEGFE
- advanced_examples.rst Spin Transport: inds defined explicitly
  (NEGF.setSigma does not return contact indices; previous code
  would crash with NoneType subscript). setSigma calls now use
  lContact/rContact/sig kwargs.

Mechanical fixes only. Of Group C's 5 mechanical-flagged findings,
2 were applied here. The other 3 (C.3-C.5 in phase-1-findings.md)
had verifier-proposed fixes that comment out broken example blocks --
that approach degrades the docs. Rerouted to judgment review for
proper rewrite (likely "switch to NEGFE properly" rather than comment
out).

Group C also produced a missing_modules sub-section listing 6 modules
absent from api/index.rst (surfG3D, spinTools, utils, protocols,
config, fermiSearch). These are NOT added in this commit -- they
are Phase II decisions.

Group C ethane.rst, examples/index.rst, api/index.rst had no
mechanical findings to apply.

---

## Commit candidate: Docstring fixes across gauNEGF/

Files: gauNEGF/scf.py, gauNEGF/density.py, gauNEGF/transport.py,
       gauNEGF/surfG1D.py, gauNEGF/spinTools.py

Suggested message:
docs(gauNEGF): fix docstring references to match function signatures

Docstring-only edits across 5 modules. Parameter defaults, type tags,
and missing sections brought into agreement with the API truth table.
No logic changes.

- scf.py NEGF.__init__: corrected func default ('b3pw91' -> 'hf')
  and nPulay default ('4' -> 'PULAY_MIXING_SIZE from config')
- density.py densityReal: corrected T default ('300' ->
  'TEMPERATURE from config')
- transport.py current/currentSpin/currentE: corrected T default
  ('0' -> 'TEMPERATURE from config'). Originally only `current`
  was flagged; the post-fix recheck caught currentSpin and the
  same pattern was found in currentE.
- surfG1D.py surfG.__init__: added missing `spin` parameter to
  Parameters section
- spinTools.py genOrthRots: added missing Returns section
  documenting (rotations, directions) tuple

Mechanical fixes only. The following originally-flagged items were
deferred:
- D1.1 (scf.py setFock Hartree/eV): doc-vs-code inconsistency that
  needs user adjudication; logged in phase-1-bugs.md and
  phase-1-judgment-fixes.md.
- D1.5 (density.py densityComplexN showText): verifier was wrong;
  showText IS in the function signature -- no fix needed.

The 3 judgment-level Group D findings (D1.J1 spinLockList docstring
completion, D1.J2 currentSpin spin description, D2.J1 surfG.g i
parameter JIT note) require user input and are deferred to
phase-1-judgment-fixes.md.

The 7 utility modules in Group D3 (matTools, integrate, utils,
protocols, config, fermiSearch) had no mechanical findings to apply
beyond the spinTools fix above.

---

## Commit candidate: Group A judgment fixes (README sig1/sig2 typo)

Files: README.md

Suggested message:
docs(README): fix sig1/sig2 attribute names to use getSigma()

The README's transport.current() example referenced `negf.sig1` and
`negf.sig2` -- typos for the actual attribute names `sigma1`/`sigma2`.
Replaced with the preferred `sig1, sig2 = negf.getSigma()` form which
works for both NEGF and NEGFE.

User-approved during Phase I judgment review.

---

## Commit candidate: Group B + Group C judgment fixes (NEGF/NEGFE prose + docstring)

Files: docs/source/theory/best_practices.rst,
       docs/source/examples/advanced_examples.rst,
       gauNEGF/scf.py

Suggested message:
docs: clarify NEGFE-only features and fix scf.py setFock/setDen docstrings

User-reviewed judgment fixes from Phase I:

theory/best_practices.rst:
- Add temperature example now imports NEGFE and constructs a NEGFE
  instance before calling setSigma with T=300 (T is an NEGFE-only
  override).
- Bethe Lattice and 1D Chain examples now include `negf.` prefix on
  setContactBethe / setContact1D calls (previously bare).

examples/advanced_examples.rst:
- Temperature Effects section: prose now says "Or locally on an
  NEGFE object", and the code block imports + constructs NEGFE.
- Energy-Dependent Contacts section: code block imports + constructs
  NEGFE before using NEGFE-only methods.

gauNEGF/scf.py:
- NEGF class docstring: F attribute now correctly noted as Hartree
  (with eV conversion factor), per user clarification.
- NEGF.setFock summary: "converting from Hartree to eV" -> "from eV
  to Hartree" (matches actual math).
- NEGF.setDen spinLockList docstring: completed truncated text
  "List of atoms to apply sp" -> "spin-locking to".

Verifier-flagged "judgment" items REJECTED as not actually broken
(see phase-1-judgment-fixes.md): NEGFE constructor pattern (NEGFE
inherits NEGF), setContactBethe nested-list shape (was correct),
SigmaCalculator(surfG) overload (real overload).

---

## Commit candidate: misc docstring tweaks (transport + surfG1D)

Files: gauNEGF/transport.py, gauNEGF/surfG1D.py

Suggested message:
docs(gauNEGF): minor docstring rewording on currentSpin and surfG.g

User-approved:
- transport.py currentSpin: spin parameter description now reads
  "for spin-dependent calculations" (paradoxical "restricted" wording
  removed).
- surfG1D.py surfG.g: the `i` parameter now notes "(static argument
  for JAX JIT compilation)" so users know not to vary it dynamically
  in performance-sensitive code.

---

## Commit candidate: Phase I judgment-fix log + bugs reclassification

Files: docs/superpowers/specs/phase-1-judgment-fixes.md,
       docs/superpowers/specs/phase-1-bugs.md (rewritten)

Suggested message:
docs(spec): record Phase I judgment-fix decisions and bug reclassification

phase-1-judgment-fixes.md: per-finding log of every judgment-level
issue from Phase I, with the user's decision (Approve / Reject /
Defer) and what was applied.

phase-1-bugs.md: rewritten after user review revealed the Phase I
verifier had over-interpreted the truth table on multiple counts
(class inheritance, dynamic attributes, variable-shape returns,
method overloads). Of the 8 originally-flagged "code bugs", 7 were
not actually bugs (left intact); 1 was a doc typo (fixed via
README judgment commit above). Net: zero genuine code bugs.

The bugs file now serves as a record of the verifier's known blind
spots and what to add to the truth-table generator in Phase II.
